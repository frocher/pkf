{
  "name": "@pkf/cli",
  "version": "0.1.0",
  "description": "Command-line tools for Project Knowledge Format bundles.",
  "type": "module",
  "bin": {
    "pkf": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pkf/core": "file:../pkf-core"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
